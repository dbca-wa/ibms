apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ibms-webserver-hpa
spec:
  minReplicas: 2
  maxReplicas: 5
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
  metrics:
    - resource:
        name: cpu
        target:
          type: AverageValue
          averageValue: 50m
      type: Resource
